[package]
name = "rps_simulation"
version = "0.1.0"
edition = "2024"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
avian2d = "0.6.0"
bevy = {version = "0.18.1", features = ["dynamic_linking"]}
rand = "0.10.0"
